import { LocalizeDirective } from './localize.directive';

describe('LocalizeDirective', () => {
  it('should create an instance', () => {
    const directive = new LocalizeDirective();
    expect(directive).toBeTruthy();
  });
});
